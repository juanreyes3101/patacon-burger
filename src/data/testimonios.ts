export interface Testimonio {
  nombre: string;
  texto: string;
}

// Vacío a propósito: sin reseñas reales todavía. Agregar aquí cuando existan
// (Google, redes) — la sección se muestra sola en cuanto haya al menos una.
export const testimonios: Testimonio[] = [];
